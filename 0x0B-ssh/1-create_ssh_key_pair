#!/usr/bin/env bash
# Generate the RSA key pair and a passphrase
private_key="school"
public_key="$private_key.pub"
bits=4096
passphrase="betty"
ssh-keygen -t rsa -b $bits -C "Generated by 1-create_ssh_key_pair" -N "$passphrase" -f $private_key
